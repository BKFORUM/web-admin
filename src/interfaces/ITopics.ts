export interface ITopic {
    id: string;
    name?: string;
    displayName: string;
}

export interface IFaculty {
    id: string;
    name: string;
    displayName: string;
}

export interface IOption {
    id: string;
    name: string;
}